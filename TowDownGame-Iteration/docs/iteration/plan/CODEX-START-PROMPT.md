# 直接交给Codex的启动提示词

你现在接手 `seiya058904/game-prototype-lab` 的 TowDownGame 体验增强版。请执行随附的《TowDownGame-Codex-Upgrade-Plan》计划库或其合并版 `TowDownGame-Codex-Master-Plan.md`，不要只回复建议、再写一轮计划或重复询问已确认方向。

先从当前工作目录、根README、AGENTS和已有PLAY_GAME.bat确认真实工作区。用户给过的路径线索是 `D:\xia zai\AI project\8.30 Backup game\`，参考项目在其中的 `Godot-GameTemplate`；不要假定你的cwd或路径已被验证。计划库可能在工作区 `archive\workspace-support\TowDownGame-Codex-Upgrade-Plan\`，也可能作为附件提供。

【已经确定，不能擅自更改】

1. 保留“营地购买/装配 → 进入战斗 → 结算回营地 → 再次整备”。
2. 方向是内容更丰富、打击更明确、清怪更爽，不是更硬核。保护现有移动、冲刺、美术和原激光等特色枪；不全盘重写。
3. 新试玩档金币9999、天赋点9999，集中配置。装备仍通过金币购买，天赋增加金币购买并保留点数升级，一次交易只扣所选一种货币。满级/失败不扣款。9999只用于钱包，不用于生命、伤害、天赋等级或特效数量。
4. 不做经济稀缺、刷取门槛、收费、正式发行平衡或额外完整沙盒模式。营地提供简单试玩资源补充和关卡选择，方便体验即可。
5. 消除妨碍战斗的黑暗/局部视野限制，但保留发光和美术层次；先改善可读性，不擅自把相机拉远到角色变小。
6. Godot-GameTemplate是首要的战斗反馈参考；Barren-Game次要。借鉴具体优点，不移植整套架构。
7. 原 `TowDownGame/`、`Godot-GameTemplate/`、`Barren-Game/` 全部只读。仅 `TowDownGame-Iteration/` 可改游戏内容；副本已有工作则先读取，不能覆盖重建。存档也必须与原版隔离。

【完整路线目标】

24把实际可买可用的枪（保留原10把）、24种实际可装配的配件、24种可查看且真实生效的持久天赋、12种普通/特殊敌人、3Boss、6个有差异区域、30个遭遇。具体机制、兼容、测试和依赖按计划库执行。不能把消耗品算持久天赋，不能换皮凑数量。

【这次先完成什么】

先读00、01、02、10，再执行M0–M2。核对本地版本和已有改动，建立安全副本，修复交易/经验/装卸/激光生命周期等前置问题，完成9999资源与金币买天赋，改进视野和购买装配反馈，然后做第一个新内容切片。

M2至少包含：原10枪保留；新增电弧枪、等离子榴炮、三连发卡宾；6件新配件；6种清楚可查看的持久天赋；原近战怪加蜂群、冲锋和远程三种新敌人；改良R1与3个遭遇；一条可以反复跑通的营地→战斗→返回流程。

实际执行自动化检查和真实Godot窗口验证，产出独立启动入口/Windows预览包、阶段提交及简洁试玩说明。先给可玩结果，不要在仅复制、仅审计或仅写文档后收工。

到M2完成后停在H1真人体验门，状态写READY FOR HUMAN REVIEW。不要未经用户实际反馈就一次铺开剩余全部内容，不把自动化通过写成人已认可。H1通过后再按计划继续M3–M4，H2通过后继续M5–M6。

【工程与验证底线】

- 基础参数和有效属性分开，装卸重算、UI预览与实战同源；重复装卸不能永久变强。
- 已买/未装/已装/不兼容、天赋当前等级与收益、下一次升级变化必须清楚。
- 激光按其射线/持续tick逻辑保留特色，回归切枪、暂停、死亡、回营地后的伤害/声音/粒子停止。
- 伤害派生有限深度、死亡结算幂等、异步任务有取消/代次；不能连锁失控。
- 原始目录和用户已有工作不得破坏；副本默认由外层Git作为普通源码跟踪，不复制原.git，不把gitlink当源码已上传。
- 使用工作区现有portable依赖；不全局安装，不写入私有路径到发布文件。
- 在实际本地Godot版本验证，不凭README版本字符串升级工程。测试、截图、音频和性能未验证就明确写未验证。
- 默认仅推送核对过的用户开发分支，不自动合并main、不自动创建正式发行；Windows导出是试玩构建。

把真实进度写入副本 `docs/iteration/STATUS.md`，标明已完成、未验证和阻塞。遇到技术细节自行作最小适配并记录，别把产品决策重新扔回用户；若必须破坏已有工作或改变核心方向才能继续，则只停止冲突操作，交付已有成果和准确原因。

现在开始读取计划和实际仓库，按M0–M2执行。
