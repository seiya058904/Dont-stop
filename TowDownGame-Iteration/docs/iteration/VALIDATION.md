# M0—M2 验证记录

> 最新结果见本文末M2-R1记录；前面的M2/分批记录保留为历史，不能视为当前全部验证结论。

日期：2026-09-12。STATIC / AUTOMATED / GUI / HUMAN 分开记录。

## 实际环境

Windows；Godot 4.7.2.stable.official.ed1daf0bf（工作区已有 portable），Vulkan Forward+；AMD Radeon RX 7900 XT，Ryzen 7 9700X。没有全局安装、修改 PATH 或升级依赖。

## STATIC

- 起点 e267aa449c45860c42a3d0e8ae6a26e635341ba7。BASELINE-FILES.json 记录复制的2292个源/素材/配置文件；最终重新计算原 TowDownGame 文件 SHA256，差异0。
- 外层 Git 的三个原项目差异为空。迭代版没有嵌套 .git，按普通源码目录提交。
- 原10把枪均保留。相机、角色碰撞与移动/冲刺基线保持。具体必要变化见 DECISIONS.md。
- CONTENT-MANIFEST.json 从实际资源注册表实例化生成，未把目标计划数量当作现状。

## AUTOMATED

运行：`powershell -ExecutionPolicy Bypass -File tools/verify.ps1`（在迭代目录）。最终5个进程全部 exit 0，无 SCRIPT ERROR / ERROR / FAIL / 退出泄漏。

| 检查 | 实测结果 |
| --- | --- |
| 编辑器资源导入 | 通过 |
| BaselineRegression | 4项通过；修复前实际记录4项失败：精确经验门槛、装卸累乘/恢复、商店选择残留 |
| ContractRunner | 114项，0失败；实际114条明细在 verify-2.log |
| SaveRunner write/read | 两个独立进程，1+9项通过；使用独立 contract-test-camp.json，不覆盖玩家存档 |

综合回归覆盖：双钱包/单币支付/满级与余额不足不扣费、补充保留装备天赋、13枪实际开火耗弹、6新配件兼容及不兼容、20轮装卸不漂移、同槽替换与实例跨枪归属、有效属性、连杀/吸能触发、激光射线tick去重与暂停后回调取消、三连发余弹与切枪取消、链电多目标与遮墙、爆炸范围、死亡一次结算、派生深度、3遭遇开始/结算回营、清理临时攻击、暂停栈与重复输入开关。

新加6项通过真实 Viewport 的 InputEventKey 注入，验证两轮 Tab→设置→Esc→Esc；这是引擎输入测试，不等同真人键盘验收。测试不覆盖后续阶段未实现的枪/敌人/Boss，也不声称穷举全部旧奖励组合。遭遇 ContractRunner 中加速了结算时钟，完整45秒过程另见GPU测试。

## Windows GPU 自动运行

1920×1080真实图形进程（并非headless）。帧时间为引擎每帧delta，跳过开始3秒，保留统计脚本 tests/WindowPressure.gd。无人工瞄准/生存能力结论。

| 场景 | 时长 | 活跃敌人 中位/峰值 | 活跃射弹 中位/峰值 | p50 / p95 / p99 毫秒 |
| --- | --- | --- | --- | --- |
| 普通R1遭遇自动运行 | 48.00秒 | 5 / 11 | 2 / 6 | 6.25 / 6.25 / 6.25 |
| 压力夹具 | 33.01秒 | 150 / 150 | 400 / 400 | 6.25 / 10.00 / 15.28 |

普通场景完整计时胜利并返回营地（encounter_completed=true），91次伤害事件，结束256节点、起点255、孤立节点0。夹具有自动复活与自动瞄准，不能解释成人已通关。

压力夹具维持150敌人/400实体弹，有3128次伤害事件，孤立节点0；结束仍有活跃夹具实体（4377节点），不能据此声称长期无内存增长。首次优化前同类压力p95为144.44ms；定位后限制装饰光/弹壳/数字并缓存导航，未丢弃关键伤害或射弹。

可随代码查看的精简证据已复制至 docs/iteration/evidence/（含综合断言日志、两次GPU统计及原生练枪截图）。本地原始记录：normal-gpu.log、pressure-optimized.log、evidence/normal.json、evidence/pressure.json。evidence/pressure.png 来自优化前压力，不用它证明优化后结果。

## GUI：真实原生窗口操作

已执行：1280×720与1920×1080窗口检查；最终使用PLAY_GAME.bat启动；购买原激光和电弧、金币/点数分别升级火力、购买电弧中继并装备，观察价格/余额/未装备和已装备信息；重启恢复；设置嵌套、滚动访问底部与关闭；补充钱包后装备天赋仍保留；选择R1遭遇实际进入，失败后返回营地；营地实际鼠标射击练枪靶。

最终练枪截图 evidence/native-practice.jpg 显示电弧单次耗弹及3目标的2.67/2.00/1.50递减命中数字；HUD显示电弧中继与火力2。原始截图由原生窗口工具截取，未生成/修图。

限制：原生自动化键盘对原物理键绑定触发不稳定，无法可靠确认最终窗口中的Tab、WASD和冲刺全过程；引擎输入测试通过，但仍请H1亲自验证。真实窗口失败回营已操作，计时胜利回营采用上述GPU自动流程。声音未由人耳试听，音频节点/总线测试不代表听感正常。

## 尚未执行

H1真人手感与声音验收；20分钟/10轮长期稳定性；跨硬件及Windows独立导出包干净目录测试；全组合/100seed完整可达性；M3—M6内容与最终发行验收。没有导出模板，按M2允许路径交付已验证本地启动入口。以上均未标PASS。

## M2-R1 追加更正与证据（2026-09-12）

以上M2记录是当时执行结果，不能解释为R01—R07已通过。复审实际发现等级伤害不同步、读档丢弹、弱校验、旧门重复进度、旧连发延迟取消、震屏强度截断、旧音频路由和AmberStar缓存残留；原始失败记录保存在evidence/m2-r1/before.txt及matrix-before.txt。本轮不删除历史证据，也不把H1判为通过。

| 问题 | 修复提交 | 验证 |
| --- | --- | --- |
| R01 实际击杀升级后全持有枪属性 | e9372b8 | R1Regression：未购T10/无配件/零暴击；发射后属性保留，升级后首发与备用枪真实碰撞伤害 |
| R02 完整配置后恢复弹药 | e9372b8 | R1SaveMatrix：18个独立读取进程，2枪/同型号2份配件/T04/满空半/顺序反转/连续3轮，72项弹数及归属计数断言 |
| R03 全量校验、坏档恢复、旧奖励恢复 | e9372b8、fc4da1d | Persistence 45项；校验失败不改内存，坏原文/导出字节一致，旧一次性奖励不发钱不复活；额外修正新档接管后HUD残留 |
| R04 原子存盘与错误反馈 | e9372b8 | 专用路径注入打开/写入/替换失败；上一快照字节一致，交易成功和磁盘成功分离，重试不重复扣款/发物 |
| R05 所有普通出发入口 | fc4da1d | Flow正常/试玩/重复触发/无枪/死亡/非法关卡；另4个独立进程覆盖退出重开后两条路线 |
| R06 界面同步与切枪 | a07fc69、fc4da1d | Flow 30项含同枪无动作、去抖<=0.15秒、选中/标签/滚动位置、配件实例定位、持久奖励详情、练枪清场 |
| R07 分组声音、持续攻击停止 | fc4da1d | GPU Timeline 30项，全部13枪及装填总线、旧击杀/榴弹、总静音/SFX静音、激光完整时间轴；人耳未验证 |
| V01 / V02 | fc4da1d | Regression额外验证派生死亡/直接释放/回营标记清理；Timeline实际按住输入与射线碰撞、合法连杀射速、暂停/切枪/装配/死亡/回营及旧榴弹右键 |

原有verify.ps1仍保留：5个进程通过，Baseline 4、Contract 114、Save 1+9。新增tools/verify-r1.ps1的26个headless进程均通过；强制要求summary存在且failures=0，同时检查退出码、SCRIPT ERROR、ERROR、FAIL。R1Regression最终25/25、Persistence45/45、Flow30/30。完整细目保存在evidence/m2-r1。

切枪要求已由用户明确从1.1秒改为<=0.15秒，因此原ContractRunner保留旧配置下的断言分支，并为新0.12秒配置验证实际单调时钟解锁；没有删除功能断言来掩盖失败。

headless无法捕获鼠标，原激光按住射击测试在headless失败的原因已用Input.mouse_mode=0确认；实际Windows窗口可进入模式4并产生真实命中。没有因此改动玩家的输入门槛或原物理按键。GPU时间轴首次退出出现纹理RID回收诊断，保留timeline-gpu-exit.txt；不能由节点断言推断其已解决，也不把它解释为持续泄漏。

长期测试、最终GPU压力与原生窗口复验结果将在完成后继续追加。HUMAN_ACCEPTED=false。


## M2-R1 最终复审补修与证据说明

- `876a362`：旧琥珀镰刀临时增益在读档移除时撤销（原5项测试1失败→5通过）；坏档提示置顶与已打开恢复提示的退出选项（9项中3失败→9通过）；新档等级HUD、真实替换提示和退出自定义光标纹理释放。
- `5d5b46d`：六天赋机制参数由战斗与当前/下一档详情共用，数值保持（Flow新增下一档累计值断言由失败→通过）；文件替换成功但回读损坏时恢复上一有效快照（注入readback损坏，旧实现1失败→51/51存档专项通过），`.previous`保留上一次文件。没有删改失败记录。
- 最终回归：原有verify.ps1 5个进程通过（Baseline 4、Contract 114、Save 1+9）；verify-r1.ps1 28个进程通过，含Regression 25、Persistence 51、Flow 33、Recovery UI 9、Legacy Restore 5，18个跨进程矩阵恢复的72条断言，以及4个正常/试玩独立进程。详细索引：`evidence/m2-r1/final28-index.json`。原有较早的26进程/30项Flow/45项Persistence记录保留为历史。
- 新增`R1BeamGrowth`真实碰撞：无T10、无配件、默认零暴击，先发射普通弹并切到原激光，普通弹击杀升级；同一0.4秒光束中记录到2.30（1级）→2.60、2.60（2级）。未调用Demo.refresh掩盖等级同步。

### 已完成的GPU及原生窗口证据

Godot 4.7.2.stable.official.ed1daf0bf；Windows、Ryzen 7 9700X、RX 7900 XT、Vulkan Forward+。GPU测试在用户要求停止Windows Use之前执行；其后没有再控制桌面、启动可见游戏窗口、移动系统鼠标或注入原生按键。

| 证据 | 实际结果 | 限制 |
| --- | --- | --- |
| 原激光完整时间轴及旧榴弹右键 | GPU 30/30；13枪/装填与旧音频总线、合法T10加速、实际按住射线tick、暂停/切枪/装配/死亡/回营 | 引擎输入自动化；不是人耳试听或真人验收 |
| 激光中途真实击杀升级 | GPU 2/2，后续tick立即读取2级伤害 | 单场景定向回归 |
| 150敌人/400射弹夹具 | 1536×864，33.002秒；实际活跃敌人中位/峰值150/150，射弹400/400；p50/p95/p99为6.25/8.33/11.11ms；2865次伤害 | 33秒压力样本，不能推出长期稳定或全程100FPS；结束夹具仍活跃，节点256→4325、孤立0 |
| 首轮GPU混合长测 | 1217.753秒，24次回营/保存恢复，2次复活，1236击杀；p50/p95/p99为6.264/7.429/8.034ms；敌人中位/峰值1/35、射弹0/10 | **2次清理断言失败，未标PASS**：第18/19轮有2/1个临时实体；当时缺少实体类型/出生时刻/原生输入记录，原因未定；孤立与AmberStar缓存始终0 |
| GPU短诊断 | 167.349秒、24回营、8复活，0失败 | 缩短遭遇的诊断，不能替代20分钟；后续增加实体类型/年龄/输入及清理时间序列追踪 |
| GPU退出纹理诊断 | 释放自定义光标后，Timeline、BeamGrowth、压力三个独立GPU进程的退出stderr均为空 | 早期RID诊断原文仍保留；不能由此推断所有驱动均正常 |

原生窗口实际操作使用专用测试存档路径，不接触玩家营地档：购买BoomBoi、电弧，滚动找到新枪，装备电弧；购买并安装/卸下/重装电弧中继，列表与详情同步；分别金币/点数升级火力，观察金币9589、点数9998、火力2；设置关闭震屏/降低闪光，三靶练枪实际鼠标命中、不增加钱/经验，关闭菜单点击未穿透开火。坏档原文导出和失败重试已操作；修复后恢复对话框位于营地面板之上；Alt+F4在恢复提示已打开时出现取消/放弃选项，取消后保留临时状态，显式新档前备份了原文。相关JPEG及日志保存在本目录evidence/m2-r1。

原生自动化的Tab仍未可靠触发，标题栏坐标点击出现工具定位偏差；引擎Tab/Esc回归通过，未因此改WASD/Shift/Tab或碰撞/镜头。用户要求不再占用桌面后，计划中的最后一次原生重启观察没有完成；磁盘中已检查到购买后的9589/9998、两把枪、配件归属和9发电弧弹数，但这不等于原生重启画面验收。测试中改动的声音/震屏配置文件已恢复为测试前内容。最终天赋详情文本的真实窗口布局、人耳声音及H1手感仍未确认。

### 后台验证方法与可复现入口

`R1LongRun.tscn`使用独立存档；headless时使用1536×864的SubViewport（渲染关闭），把鼠标位置注入子视口，按武器自身can_shoot/装填/计时器驱动真实_shoot，保留实际弹道、射线和物理碰撞。这样不依赖桌面鼠标捕获。根headless窗口不能更新鼠标位置的探针失败记录与SubViewport修正后的通过记录均保留；没有修改产品开火输入门槛。此模式的帧间隔是受`--max-fps 160`约束的后台循环时间，**不是GPU渲染帧率或声音证据**。

在仓库根目录运行：

```powershell
& '.\archive\workspace-support\_tools\godot\4.7.2\Godot_v4.7.2-stable_win64_console.exe' --headless --max-fps 160 --path '.\TowDownGame-Iteration' res://tests/R1LongRun.tscn
```

默认需实际运行至少1205秒并完成至少10次回营；覆盖全部13枪、装卸、两种货币升级、练枪转正式战斗、每轮购买补给/保存恢复、暂停和失败复活。`-- diagnostic`是24次短遭遇诊断，不能充当20分钟报告。所有结果须同时检查JSON的complete/failures及stderr；不能仅凭退出码0判成功。每轮回营2秒的同一清理点记录节点、孤立节点、临时实体、敌人、音频声部、AmberStar缓存；测试自己的帧样本数组会增长，不能把它当作游戏实体泄漏。

后台20分钟结果及最新源码的24轮短诊断完成后继续追加。该长测在`876a362`的运行实例上开始，随后`5d5b46d`只集中相同天赋数值并新增磁盘回读回滚；后者另以全量存档矩阵和最新源码短诊断验证，不声称长测进程热加载了之后的修改。


### 后台混合流程最终结果

完整复测实跑1221.397秒，24次回营、24次保存恢复、24次战斗中暂停；全部13枪轮换，练枪均有实际命中且不增加XP；1525次伤害、1203次击杀、1次失败复活。流程/清理断言0失败。每轮相同清理点：节点371—411，孤立节点0，临时实体0，敌人0，AmberStar缓存0，音频播放节点恒为1（营地音乐）。节点数随当前武器、短动画和界面变化，没有从这些采样点观察到持续累积；这不是内存无泄漏的证明。

后台循环p50/p95/p99为6.900/7.228/7.473ms，176835帧；实际活跃敌人中位/峰值1/14，射弹0/9。无窗口，1536×864 SubViewport关闭渲染、max-fps=160；Windows 11企业版10.0.22631，Ryzen 7 9700X（8核16线程），系统可见内存约31.6GiB。期间并行运行相关headless回归并整理文件，用户桌面应用继续运行，数据仅对应此机器负载，不能视为GPU性能或全程100FPS。

**退出诊断单独记录：** 此20分钟进程结束时报告2个ObjectDB对象和1个资源仍被引用。因此这里只标“流程和清理断言通过”，不标整个进程无错误；详细原文为background-20min-exit.txt。首次GPU长测的2次临时实体残留在此次后台隔离运行未复现，但后台测试不能替代同环境GPU复验，原因仍未确定。

最新游戏源码5d5b46d另运行24轮短诊断：168.220秒、24次恢复、8次复活，0失败、退出stderr为空；相同清理点节点371—403，孤立/临时实体/缓存均0，播放节点恒1。它覆盖最新快照回读备份逻辑，不替代上述20分钟时长。其后台循环p50/p95/p99为6.900/7.147/7.342ms，实际敌人中位/峰值1/3、射弹0/10。两类结果原文与JSON均保留。


补充退出复验：在最新源码上读取长测营地档，三连发、Baby、狙击各跑一个完整45秒遭遇，含暂停、失败复活和回营，以`--headless --verbose`执行R1ExitDiagnostic。实跑143.795秒、7项通过，退出stderr为空，未复现2对象/1资源诊断。没有把未复现等同修复；该长测退出诊断和首轮GPU残留均保留为已知待复验事项。

### 最终交付边界

M2-R1已交付 / READY FOR HUMAN REVIEW；HUMAN_ACCEPTED=false。13枪、15配件、6计划持久天赋、4敌人、改良R1/3遭遇保留，原型10持久奖励与2一次性物品另计。未启动M3—M6；未合并main或制作正式发行。没有匹配导出模板，交付工作区PLAY_GAME.bat；最后一轮没有再次打开它以免影响用户桌面，启动脚本未改、实际portable存在且最终headless导入/主场景与流程检查通过。不能把旧Game.exe当作本轮导出结果。

仍待验证：H1实际键盘与移动/冲刺手感、最终详情布局、人耳分组音量/原枪音色；首轮GPU长测2次临时实体残留的同环境复验；后台长测退出2对象/1资源诊断的稳定复现与定位；其他硬件/独立Windows导出目录。用户要求停止Windows Use之后均采用后台执行，没有再操作其桌面。

最终源文件边界核对：TowDownGame原2292文件SHA256全部一致；三个原目录在Git中无差异；用户计划ZIP保留。原生QA设置已逐字节比对回到测试前状态。证据见boundaries-final.json；开发分支feat/towdown-experience-upgrade，源码补修至5d5b46d，后台夹具0fdc476；交付文档及证据随最终提交推送。
