# 执行决策与必要适配

## M11 当前决策（覆盖下方历史停止点）

- 第四轮真人反馈授权继续 M11，起点 `928db1c`；冻结 24/24/24/24/12/3/6/30，保留已有全局强化与 Attachment 合并合同，不虚构额外装备加成。
- 难度主基准改为固定 earned budget 的 A/B/C 合法购买 + active fire；无射击只作容量与安全诊断。27–29 通过重叠到场和 E01/E02 速度 +20% 加压，不加 HP、不削弱全局玩家伤害。实际提升超出部分建议百分比，明确记录，交由真人判断容错。
- 修复已复现的贴墙玩家格点被保守导航判为障碍时停止增援的问题；出生距离、方向、路径和 encounter cap 继续强制检查。
- 原 M10 升级没有 full heal。M11 明确定义最大/当前 HP 各 +0.5，与每级伤害 +0.3、奖励点 +1 共用 LevelProgression；不把营地补给的治疗归因升级。
- StatLedger 在生产 EffectiveStats 计算处记录来源；伤害 UI 对齐下一次真实 shot_context，条件、百分点和独立倍率分开。旧档无法追溯的累计生命明确列历史差额，不捏造购买归属或重复授予。
- 使用生产 B02 巨卵实现显式“束缚教学”，不直接注入 root 伪造碰撞。角色前景绘制避免巨卵遮挡；0.45 秒束缚/1.2 秒免控及射击装填合同保持。
- 原生 OpenGL 后台显式绘制、无焦点/鼠标捕获；CPU 与帧间隔、冷启动和稳定段分开。自动站桩可存活、偶发 Boss 死亡和性能 spike 均保留；HUMAN_ACCEPTED=false。
- M11 分批提交并推送当前 feature，完成 0/0 检查后停止，不进入 M12、不合并 main、不发行。详见 [M11](M11-HORDE-CLARITY.md)。

## M8 历史决策

- 起点 `55a22ae`，继续 `feat/towdown-experience-upgrade`；只改迭代目录。内容数量与原项目保持不变，不合并 main、不发行、不进入 M9。
- 先保存原型/M5 行为与合法高阶伤害链基线，再调整已有战斗。三枪死亡的已复现主因是 150–180 HP 的旧 Boss 与现有 Tier V 火力不匹配；未发现需要先修复的重复 callback。伤害逐事件证据与边界见 `evidence/m8/baseline-damage.json`。
- 后期难度通过恢复期移动、推进/侧翼/追击、混合角色队列和有限召唤提高。保留普通入伤 0.875 与 Boss 入伤 0.97，不继续全局削弱，不增加普通敌人 HP。
- 修复 Hero 碰撞掩码漏掉生成区域墙层，阻止玩家出界导致敌人被墙隔开。保持角色碰撞形状、普通移动和 Shift 速度/时长不变。
- 三 Boss 每个三种主动攻击、两阶段、持续重新定位。完成伤害诊断与真实 TTK 后，生命预算收敛为 4400/6000/6200，Boss 1 护甲 120；没有按枪限伤或强制计时。
- 24 旧配件改为 24 个唯一永久全局状态，保存 schema 6 / `owned_global_upgrades`。第一次购买即时刷新所有枪并保存，未来枪读取相同状态。旧合法拥有按定义折叠，重复不叠层、不退款；保留旧 ID 和仅供旧档验证/历史资源使用的字段，当前没有实例安装入口。
- 营地底部快捷购买排删除，E 打开五页菜单。武器强化只显示名称、效果、价格、激活状态；取消安装、卸下、槽位、兼容和顺序管理。原型多发散弹/连发弹药消耗继续保留，全局回填每次只返还一发。
- 共享程序化危险填充、计时、方向和边缘；VFX 上限 32、寿命 0.28 秒，火花批量绘制，敌弹尾迹最多 7 点，不添加遮屏白闪。截图和性能采用原生 GL 后台渲染，Vulkan 离屏失败如实保留；不外推到前台或其他硬件。
- 自动操作不是真人验收。结束条件和最终数字以 `STATUS.md`、`evidence/m8/README.md` 为准；`HUMAN_ACCEPTED = false`，等待第三次真人试玩。

以下为 M0—M7 历史决定；与上述 M8 冲突的装配/库存/状态定义不再有效。

- 真实根仓库：game-prototype-lab，起点 e267aa449c45860c42a3d0e8ae6a26e635341ba7。原三目录只读；迭代目录是外层普通文件，不是 gitlink。计划 ZIP 原件未修改。
- 使用本地实测 4.7.2.stable.official.ed1daf0bf。首次编辑器导入自动写入 4.7 feature 与兼容标志，仅发生在副本。停止编辑器插件改写 autoload UID，保留 res 路径。
- 旧 GodotSteam 扩展未被实际游戏引用，副本内关闭扫描；不带原 Steam AppID 和旧 Game.exe。PNG图集已完整提供，关闭缺少外部Aseprite程序的编辑器导入插件。
- 原 SmoothPixel shader 采样辅助函数在本机4.7编译失败；内联同一算法，不改变像素滤波设计。旧 VirtualJoystick 名称与当前引擎原生类冲突，仅改副本类名。
- 移动基线：SPEED=100；冲刺速度600、时长0.06秒；碰撞轮廓与相机缩放未调整。
- 切枪锁原为0～1.5秒，取决于全局90帧取模；现在固定1.1秒，对应既有HUD切枪动画0.3+0.5+0.3。重装弹独立计时，不修改各枪原装填基础值。
- 原激光仍为射线、0.1秒tick、0.4秒开火窗口。删除临时Bullet对象，用轻量伤害载荷。基线每tick2.0未加玩家基础伤害，现在一级无增益为2.3，与实体弹遵守相同伤害来源；这是修复遗漏，不削弱旧枪。
- 伤害/容量/装填由有效快照计算：基础+固定容量，然后百分比加成，再独立乘法与边界。缩小弹匣时溢出退回备弹；增加容量不自动补满。
- R1沿用原镇区区块；新增基于实际地面和墙体的共享导航、安全生成与三段节奏。只开放1、3、4号遭遇，正常进度独立于直接试玩。
- 环境色从约0.039灰改为(0.64,0.69,0.76)，保留原色调与发光。原黑色怪物增加低强度内轮廓，受击短闪0.08秒。没有扩大角色或缩放视角。
- 模板反馈适配：独立受击/击杀音，0.2秒数字聚合，固定8个低优先音效声部；保留原枪声。未照搬模板的可无限扩容声池。
- 150敌人/400射弹测试曾暴露装饰负担。现在实体弹动态灯上限16、弹壳48、伤害数字90；只限制装饰，不丢射弹/伤害/敌人。敌人寻路路径最多每0.2秒重算（接近路径节点时可提前）。
- 原型奖励保留在独立页；本轮新6天赋与旧奖励逐条区分，2种一次性物品不计持久天赋。完整天赋重置/按原货币返还留在M4。
- 最终差异检查发现复制源中的行尾空格/多余文件尾空行，仅在副本内机械清理；启动bat明确保留CRLF。
- 购买、钱包补充与练枪生成限制在CAMP；战斗配置仍可查看/切枪/装卸。避免战斗中交易绕过营地快照而退出丢失。

## M2-R1 独立手感调整：切枪

旧值1.1秒来自HUD动画总时长；新输入去抖0.12秒，集中在DemoConfig.SWITCH_SECONDS。动画不再决定输入锁；所有快捷键/配置按钮经过PlayerData.changeWeapon，同枪请求不取消射击或装填。计时用单调时钟，暂停菜单内不冻结去抖。仅切换到另一把枪时取消旧枪动作。WASD、冲刺、碰撞、镜头与HUD动画时长不改。真人对比待H1。

M2-R1 存档升级为schema 2；v1缺失legacy可归一化，存在但格式错误则拒绝。一次性原型奖励不重发；BlueBacteria旧档按持久HP增量迁移计数，新档明确保存。来源未知ID及冲突配置拒绝整体加载。保存临时文件写入/flush/回读后原子替换，错误不重复购买。

R01等级事件只刷新有效属性，不重启动作计时器。V01已复现目标释放后标记残留，目标销毁/死亡清理独立于击杀奖励。V02原Baby三连发已复现暂停后补射，加入动作代号取消；激光仍是原0.1秒tick/0.4秒窗口。Camera2D将0.35强度截成整数0的判断修正，不改变镜头尺寸和移动逻辑。

原ContractRunner中1.1秒断言仅适用于旧配置：保留旧分支用于对照，为本轮明确授权的<=0.15秒加入独立输入断言；单调时钟去抖采用真实时钟等待，不能用手动传delta模拟。


M2-R1 复验补修：琥珀镰刀临时射速增益在奖励实例移除时幂等撤销，防止读档将临时增益固化；不改变其触发节奏或数值。坏档恢复提示延后到营地面板创建后显示，退出请求可将已存在恢复提示升级为含取消/放弃的退出提示。新档HUD初始显示等级与经验；仅发生真实槽位替换时提示旧件返回背包。退出前释放自定义鼠标纹理，修复GPU退出RID诊断，不改游戏内光标。

用户要求后续不使用Windows Use，避免影响其电脑操作。后续验证采用headless与SubViewport内的引擎输入；开火夹具保留武器自身射速/装填计时，调用真实射击、射线与碰撞，不改产品输入门槛。后台数据不能代表GPU帧率、原生键盘或声音听感。此前原生窗口证据保留，未完成部分不补写PASS。


R06最终复审：六天赋的每级数值、触发层数/时长与冷却集中到DemoConfig；EffectiveStats、击杀触发和详情/下一等级文本都读取同一配置。原数值逐项保持，UI明确显示下一档累计效果。R04追加故障注入：替换返回成功但最终回读内容损坏，原实现丢失上一快照（实测1项失败）；现在替换前保留并逐字节核对.previous，回读失败时用临时文件原子恢复上一快照，回滚失败仍保留备份。购买仍成功且标记未保存，重试不扣第二次钱。


## M3—M4（2026-09-12）

- 最新用户授权覆盖历史H1停止门：H1_STATUS=DEFERRED_BY_USER，HUMAN_ACCEPTED=false，AUTHORIZED_SCOPE=M3—M4；不把继续开发解释为手感认可。不推进M5/M6。
- 基线0ab53f4，启动时与origin一致，仅用户计划ZIP未跟踪；没有回退、重建副本或改三个原目录。ef08da9记录基线；e49b99c第一批实体枪；6e9b669第二批能量枪。第三批与配件/天赋共用快照字段一并形成后续可运行提交。
- 新枪稳定映射为计划Wxx→运行时100+xx；原0—9与已交付112/114/123不重排。枪体使用已有Tiny Alchemist像素素材；弹道/场/束/锯盘轮廓采用程序化效果，没有替换角色素材。
- 复读首要参考ActorDamage的伤害/死亡分层、短闪重触发，保留现有适配；次要参考BulletBehavior的主体/尾迹分离，继续使用真实物理碰撞和有限寿命，不搬其整体框架。
- MechanismGun复用BaseGun装填/暂停/选择/音频，MechanismProjectile负责有限反弹、裂片、导弹、锯盘；GravityField有碰撞拉拽、Boss免拉拽、定时一次爆炸。没有重写移动框架。
- 在途普通弹/特殊实体弹/爆炸持有发射属性快照；修正旧等离子爆炸读取当前枪半径的遗漏。原激光仍按0.1秒tick读取实时成长伤害，窗口0.4秒；新T05改变射程，默认射线1000保持。原激光不接A11/A13，因为其历史中心射线无宽度判定；UI明确兼容范围。
- 轨道最多6个基础目标，A18/T13可增加到总上限8；每束/每程去重，墙始终阻断。转管6→24发/秒封顶；T02对热流保持0.1秒tick并提高每tick伤害。所有派生上限2，裂片只一代；静电/回响/爆破的派生不重新触发自身。
- 新增配件参数来自AttachmentCatalog；EffectiveStats用于战斗与装配预览。A16只改局部枪体回正时间，不修改屏幕震动；A24直接击杀按发射来源给该枪返弹，不超容量，0.2秒冷却。
- 24计划天赋由DemoConfig定义参数、等级上限与触发说明；原头盔/蓝靴保留为T07/T08的历史来源，新商店不重复售卖。旧奖励节点/ID/历史数值不抹除，其他8种独特持久奖励另计；2个一次性物品不计天赋。旧历史来源不纳入计划天赋重置退款。
- schema3保存每次计划天赋购买的id/等级/实际币种/金额。v1/v2缺历史时迁移为空付款记录、保留等级；重置确认明确显示无法精确返还的历史等级，取消不变。退款和效果撤销同一事务，以revision阻止重复点击；恢复不发满资源。新字段拒绝缺项、负数、重复付款等级等损坏。
- 保留1—7快捷键，HUD仍最多7个持有栏图标，全部枪通过Tab页访问；购买配件现在保留商店标签与滚动，详情定位刚买的实例。R1Flow对应断言按用户此次明确要求更新，保留选择/实例/滚动/装卸同步断言。
- 旧Contract的13/15计数更新为24/24；未删失败用例。schema3首次跨进程JSON数值成为float导致版本集合拒绝，已先验证整数再转换，恢复专项实测通过。
- 后台验证不使用Windows Use、系统键鼠或可见游戏窗口。短测/长测出现的2对象/1资源退出诊断独立记录；verbose短复验有未复现样本，不等于修复。禁止为了零警告进行无边界重构。

- 追加实测校正：W16使用独立固定tick累计时钟，不再受单发武器Timer→下一process帧的串联延迟影响。30/160后台帧上限两秒均20tick；六种取消路径清除余量。只修改热流，新旧其他枪门槛不变；最终20分钟在此修正后重启。修正前长测记录保留，不当作最终结果。

- 新枪连续开火局部形变回正锚点经30/160样本确认偏移，改为固定场景原位置；旧枪与角色框架保持。最后混合长测从该小修提交重新计时，前两次部分运行均保留。

- 最后长测完成1220.874秒/24回营，流程断言0失败，但W22去程目标销毁后枚举物理碰撞例外出现空对象诊断。稳定短复现后只改为按已命中实例ID移除有效目标例外；既有回程去重不变。按用户避免无限整改要求，只补相关射弹/组合/24轮短诊断，保留完整长测原文并明确版本差异，不把之前长测说成包含后续小修。

- 提交边界：ef08da9基线/授权，e49b99c第一批，6e9b669第二批，64fcbd7第三批及M4，9f77778局部回正，33da09b销毁目标回返防护。H2交付文档/证据另提交；全部推送现有feat/towdown-experience-upgrade，不合并main、不发行。最终停在M3—M4 IMPLEMENTED / READY FOR HUMAN REVIEW，H1暂缓、H2待验收、HUMAN_ACCEPTED=false。


## M5 用户授权与首批集成

用户指定M3—M4为ENGINEERING ACCEPTED，H1暂缓、HUMAN_ACCEPTED=false。继续原分支，不进入M6。按08/09原目录补齐E03/E06—E12；E10交替精确直射与落点炮击，E03重冲锋，B03加入可预告侧移。共享BaseMonster、Combat、既有AStarGrid2D/生命周期；M5Content统一12敌人/3Boss/6区/30遭遇元数据。R1保留原街区，R2—R6是有真实墙体与导航的灰盒场地，不声称最终美术。每轮仍回营，Boss击杀胜利，无硬超时；schema4新增明确通关布尔，旧档默认false不猜历史。

首轮实际敌人交战70项及30遭遇/3Boss流程153项通过；原始失败记录保留。修正自爆起爆距离超出实际爆炸范围；有限候选遍历替代近距离召唤32次随机碰运气。测试射击改用已有机制枪，保留原鼠标开火绑定；受到伤害的定向测试在实际交战后移入无遮拦夹具，避免夹具枪口落在地图墙内。0伤害预告不应调用会将小于1伤害提升为1的玩家入口，HostileZone显式排除0伤害。

M5限定补修：Boss增加11像素独立碰撞体，与新区导航13像素余量一致；600个固定seed同时验证真实物理无碰撞。Boss/玩家同帧死亡实际复现复活后无法结算，用当前epoch记录待结算胜利，复活后一次结算，放弃不领奖励。短测43项通过。UI夹具直接退出时6对象/3资源，verbose明确是WAV/MP3流及播放实例；改走产品已有退出入口后15项通过且该次无诊断，旧记录保留，不推断普遍修复。第1轮后正常进入新补齐第2轮，旧跨进程断言由3明确调整为2，其他一次性出发与试玩隔离断言保留。

冻结期仅修正M4Mechanics的物理采样夹具：A23转向角按实际弹体物理age归一化，消除0.06秒主线程等待跨不同物理tick的误差；未修改武器数值或产品代码。原失败、独立未改重跑、两次修正重跑与完整575项通过记录全部保留。

M5最终统一长回归使用a3307dd冻结游戏版本，1246秒/27回营，零流程失败与运行时异常，覆盖此前锯盘销毁目标修复。清理采样未见持续实体/资源累积，退出2对象/1资源诊断保留；测试自身样本数组计入内存，不宣称无泄漏。合成压力未达到33.3ms p99参考预算，headless结果不作为GPU验收。交付停在M5 IMPLEMENTED / READY FOR DEFERRED HUMAN REVIEW，H1_STATUS=DEFERRED_BY_USER，HUMAN_ACCEPTED=false；等待下一指令，不进入M6。


## M6 工程收敛决策

1. 冻结全部内容数量，不改AI/武器/敌人伤害、生成压力或遭遇胜利条件。普通遭遇是45秒生存，Boss才是击杀清场；报告分别记录。
2. 先保留并重跑M5压力，再用临时同步函数scope计时归因。插桩文件逐字节恢复；Boss另在隔离项目测量。仅进行低风险缓存/视觉状态更新，剩余碰撞与高密度绘制成本如实保留。
3. clear_line旧的数量判断无法检测同数量实体替换；以node_added/tree_exiting使RID缓存失效，新实体同帧可见。没有省略射线判定或改变碰撞层。
4. BaseMonster仅在闪烁状态变化时更新shader、scale及接触标记；Tactical/Demo敌人的物理帧绘制保留。受击时长、死亡、DOT和特殊机制未变。
5. 旧R1测试中1→3是仅开放1/3/4的历史预期。现30正式遭遇应为1→2，修正3个过时比较值，保留原断言及重复进门/试玩隔离检查；不改变生产进度逻辑。
6. 退出2对象/1资源按真实身份分类B。对应Godot精确commit的AudioServer异步释放源码和最小复现实验见evidence/m6/audio-classification.md。未屏蔽诊断或升级引擎。
7. 短机制测试区分未拾取金币与攻击节点；金币允许在战斗中等待拾取，但回营统一清理仍必须全部为空。没有删除奖励或减少测试断言。
8. 遭遇审计的机器人死亡不是自动等价于游戏阻塞。第13关以普通移动靠近的补充实验验证可完成，保留原90像素距离策略失败记录。没有隐藏增伤、无敌或强制胜利。
9. 所有后续验证保持headless，不使用前台computer use或系统键鼠。一次已完成的12轮GPU资源复验与30分钟headless长测分开陈述；真人音画、审美和手感仍未验证。
10. 原三个项目与用户ZIP保持边界。仅推送现有开发分支，不合并main或发行。导出模板不存在，preview build非阻塞，PLAY_GAME.bat不改。


## M7 真人反馈对应决策

- 长说明藏购买按钮 → 两列商品浏览、默认短卡、折叠完整属性、独立固定操作栏；1366×768逻辑尺寸逐枪实际交易检查与离屏渲染分别验证。
- 分类和补弹入口不明显 → 高亮一级导航，弹匣补给独立；营地五个常用操作直接可见。
- 固定子弹补给不公平 → 备用库存唯一单位reserve_magazines。完成换弹扣1并补满，提前换弹同规则；取消不扣，满弹不扣。换枪/缩容不返还虚构弹匣。原T11调整为每5杀补等级数个弹匣。
- 开局买光、价格无成长 → 新档180金币/0天赋点/10弹匣，开发补钱仍有明确标签；旧合法钱包不擅自降低。固定Tier和不重叠价格带，武器数值修订单独记录，不用隐藏玩家增伤假装降难度。
- 武器缺少强弱层次 → EffectiveStats集中应用各枪固定倍率，保持机制和数量；实测近距三目标伤害、首秒/持续窗口、射程、装填、机制优势与局限。不同Tier允许场景专长，不以一个DPS数字代替全场景强度。
- 高阶反馈不足 → 每枪复用一个短时枪口节点，较高Tier增加有限细尾迹、局部命中弧线和受设置约束的小冲量。保留危险预告层级、低闪烁设置，没有全屏闪白或无限粒子。
- 配件认知负担 → 24×24全部支持，保留槽位/实例。旧扩容变百分比；原专用件有通用伤害或换弹收益并保留适用机制的额外加成。无效空枪对象仍拒绝，不把unsupported对象当成正式武器。
- 存档迁移 → schema5只存整弹匣。schema1—4按原枪基础+旧平加配件+持久携弹天赋容量向上换算一次；无装备基准30。旧超容量枪内子弹安全夹取，实例/货币/付款不重发。非法引用仍安全拒绝。生存模式临时buff原本不在营地存档协议中，不伪造不存在的历史字段。
- 真人偏难 → 所有已标识敌人来源的普通命中×0.875、Boss本体×0.97，置于既有防御处理之后；不增加敌人HP，不添加难度菜单。实际伤害事件记录削减前后值，区分武器成长/构筑变化与敌方压力削减的贡献。
- 旧回归 → 显式提供测试钱包，普通初始钱包另保留断言；按新规则更新兼容/价格/弹匣/固定操作栏预期。伤害断言遵循既有0.01量化；反弹盾靶提高夹具HP以继续检查单次命中，正式遭遇敌人HP不变。保留首次失败和修正后的最终日志。
- Git和验收 → 继续feat/towdown-experience-upgrade分批提交推送，三个原项目只读、ZIP不提交、main不合并、无Release。FEEDBACK_RECEIVED在工程门全部通过后才改FEEDBACK_ADDRESSED；HUMAN_ACCEPTED始终false。

- 性能补查：极端补齐夹具约3700枚金币成为额外负担；Gold.gd只缓存按等级变化的吸附半径平方，保留数量/金额/位置/遮墙/动态等级规则，收取后停轮询。对照保留首轮退化和后续全部样本，不宣称固定加速或稳定帧预算。
- 上传传输：本地Git HTTPS握手失败，GitHub API可用；通过Git Data API上传完全相同的Git对象并验证tree与commit SHA，只fast-forward当前分支，不改全局代理/依赖/remote配置。

## M7 试玩退出与钱包补修

用户进一步确认当前是试玩版，覆盖此前低钱包决定：新档金币/天赋点9999，每次真实启动在合法恢复后补至至少9999，普通保存/读取仍保留消费。坏档不覆盖，高余额不降低。新版DemoSettings遗漏退出入口，现增加固定可见退出按钮；根窗口close_requested直接连接统一保存/退出流程，退出收尾防重复。保留战斗未结算及写盘失败时的取消/明确放弃选择。

## M12 — 2026-09-13

- 冻结 M11 内容和主曲线；只处理来源表达、教学流程、商店呈现与本地测试副本生命周期。
- 属性 UI 不实现第二份伤害、暴击或射速公式。最终值来自 EffectiveStats.inspect；分类合计按账本操作展示。配件当前贡献调用同一个 calculate，展示移除该配件的边际差值，并说明存在交互、不可简单相加。
- 不将 proc、条件触发、目标限定效果冒充常驻属性；保留历史成长差额，不伪造旧档来源。
- 统一 BuildIcon 保留正式素材引用；仅使用 AtlasTexture 的透明边界裁切展示，nearest 过滤，不创建第二套图片。tooltip 由游戏视口内统一控件负责边缘换向与夹紧。
- 所有 M9–M11 完整临时副本均属可再生执行材料。正式日志/截图/JSON 保留；成功、失败及 Python 正常异常退出清理临时目录；默认保留零份失败完整副本。强制杀死 Python/断电不保证 atexit 执行，需检查 test-temp 后按同样的精确边界处理，不声称能抵御操作系统强制终止。
- --baseline 分别从 b2f8f48 / 3946250 / 928db1c 的 Git 源码重建 M9 / M10 / M11 runner 的历史基线，重建固定 Git 基线。历史运行源码 hash 与这些 Git 锚点并非全部相同，不能冒称逐字重放历史快照。
- 原生鼠标工具不可用时保留该项待验收；自动输入、渲染截图和真人接受是不同证据。不得因此设置 HUMAN_ACCEPTED=true 或宣布全部退出条件已满足。
