# B19.1 bounded protocol

Single agent. HUMAN_ACCEPTED=false. No main/merge/deploy/release.

Original B19 preserved at output/b19-1/prechange; original B18 builds remain untouched.
Source variant and workload scenario are independent. Legacy A/B/C/D retain their original meaning.

Correct-reference first fixes translation, per-frame visibility/following, body-edge flames, group-cache correctness, real physics-tick input and test exit. Performance candidate changes rendering implementation only.

Fixed seeds: 20260920, 20260921, 20260922. At most two optimization selections.
Admission: one run each of 39 ordinary horde (B), 39 barrage mix (D), 40 boss (A, 90 simulation seconds maximum). Stage 39 retains its complete 45 simulation second round. Load admission must include nonzero real damage and requested sources; no claim from cap or granted rewards.
Formal three-seed before/after only after load admission. If sources/status effects/capacity are not attained, report NOT_ATTAINED, retain diagnosis, do not repeat until lucky.
Capacity claims require 60 sustained seconds near declared target; no such claim from ordinary scenarios.
Full frame timeline retained. Opening: first 5 wall seconds of each combat round; warm: >=5 seconds. Gate p95<=18.5ms, p99<=25ms, >33.3ms<0.5%; retain >50ms and full timeline. Timing is CPU-side process interval, never GPU presentation. Monitor proxies overlap. GPU runs sequential; no parallel native tests during timed samples. Visual captures separate.
Release static-memory/orphan monitor values are unavailable, even if legacy log says zero. Render object monitor is not CanvasItem count.

## User steering during execution (supersedes the matrix above)

The user rejected repeated testing and the fire/ring enchantment art, requesting Minecraft-like pixel enchantment glint on the monster body. Cancel remaining reference matrix. Do not claim equivalent-picture speedup against rejected art. Deliver the body shader, one bounded headed Web workload check, targeted contracts and playable exports. Keep broader performance/capacity/Boss/old-regression gates PARTIAL/NOT_RUN. The superseded polygon/mesh experiment is archived outside production.


## Subsequent user correction: performance remains the primary task

The user explicitly rejected stopping at the visual candidate. The earlier paragraph's cancellation of the performance objective was an execution error. Resume real optimization while reducing repetitive tests. Record all bounded diagnostic iterations, including regressions. Keep seed20260920 and opening/warm windows fixed; real stage39 D sources are now admitted through production promotion with explicitly held elite HP. Stage40 ran90simulation seconds; Windows Release B ran a complete stage39. Three-seed formal comparison and capacity remain NOT_RUN/NOT_ATTAINED, never inferred from these diagnostic samples. The final delivery includes cache-exit correctness after Web diagnostic builds, disclosed in manifest/report.
