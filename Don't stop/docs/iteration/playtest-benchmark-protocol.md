# B14 移动清怪评测协议 v1（执行前固定）

基于装卸修复66f69e2与本轮已复现的裂片/T10修复，先建立参数调整前的参考。
不使用旧GPS倒推品质，不把自动观察器当真人通关或性能通过。

核心：24把全部测试；优先6/112/113/115/119/124，再其余18把。每把裸装与同一后期构筑各一次，
固定seed20260920，R8真实墙体与导航，15秒；随后六把重点补第二seed20260921。
第一轮只作同条件筛选，品质决定还需结合机制专项和真实关卡。

固定候选敌群：先只测E01/E02普通追击（各半），用于隔离清怪能力；不是声称最终关卡全是普通怪。
沿用真实39关普通怪基础属性，固定追击倍率1.15；每轮起始12只，之后每秒补6只、最多64活怪，
合法出生环140–210像素；不人为排成直线，不按不同武器改变HP或生成量。
使用实际碰撞、装填、蓄力、暖机、弹丸与死亡触发。既有M8合法移动/危险方向选择器和最近目标瞄准，
不传送走位、不用瞬杀或理想队列帮助轨道炮。全蓄轨道炮为核心策略，轻点/半蓄与取消另列。

裸装：Lv1，无天赋/强化。后期：Lv20，T01/T02/T03/T04各3级，T08为3级，
T10为1级；强化0/1/111/115/118/120。每轮弹匣满、备用100；观察器恢复HP以完整记录15秒，
受伤累计单列，不能当生存通过。每次真实击杀后清除测试经验，避免前一把/前一轮升级影响后续。

采集：击杀/s、首杀时间、前3秒击杀、平均/峰值/结束活怪、最近敌距、近身压力时间、
位移、受伤、真实攻击和命中事件、弹药消耗、装填次数/时间、蓄力时间、
每次击杀过量伤害、实际角色HP/速度、完整构筑和有效属性。动态生成有上限会反压，必须同时报告生成数量。
固定波次清理、真实Boss/精英、墙损耗、被迫取消与机制触发在后续扩展中单独报告，
不把核心15秒行冒称为这些检查已经完成。

旧B12超时换算已修正为damage/(30*seconds)。已保存before/after各24条crowd均未超时，
所以该分支错误不能解释那两份已保存排名；原日志/排名不重写。

本轮测量的是新玩法候选清怪能力，不是固定负载渲染性能。禁止与录屏/压缩并行。
发现夹具错误时保留失败记录、说明修复并重跑受影响集合；不择优挑选或测到通过。
## 有限扩展（运行前定义）

核心完成后，重点六把各做一次 seed=20260921 的固定24只普通移动怪波次
（18秒上限，无后续增援）、真实E03精英与真实Stage40前20秒。角色等级20，
生命按5+19×0.5=14.5设置，随后不锁血，装备与核心late一致。使用同一正常走位控制器，
Boss保留AI、地形、伤害、迷雾和真实HP，不用高血量静止假靶冒充。
另用轨道炮半蓄策略同波次对照。清场超时如实保留；Boss短观察不叫通关。

真实遭遇比较用29/39关、seed=20260922、115、等级20、late构筑加T19/最大T24，
按45秒完整关卡计时，正常HP/自动走位，不改生产生成器。记录各出生来源、出生后实际速度、
精英标记、每秒存活构成与逐帧积分的普通存活时间比例，不以生成表替代同屏观察。
新怪潮落地后补21/25/35及同条件29/39，不把减少特殊怪当作同负载优化。

核心参考日志的 `attacks` 是已有 `Combat.attacks` 计数器；6和123未接入该计数，
其0值不能解释为没开火。对6/123的发射次数列报告N/A，另列真实消耗弹药与命中/击杀数。
核心实际记录的E01/E02 HP为2/1.2、速度103.5/105（出生后的实值）。
发现旧生成链中初始化覆盖HP/速度的行为；本轮核心保持原行为，不能宣称乘上了Hell HP轴。
后续普通怪速度修正只影响新遭遇；固定核心测试须显式保持该已记录速度，才能同条件复测。

源码身份：参考运行基于66f69e2加已列明的裂片/T10修复；具体文件SHA256存于
`evidence/visual-upgrade-20260919/b14-benchmark-reference-source.json`，不是旧v12原样构建。

## B14 对旧冻结强度契约的替换（数值校准后、运行新门槛前声明）

用户执行文档第186行明确允许在新玩法预期与真实结果建立后更新测试/manifest。
旧B12的静止靶GPS逐档20%/25%间隔、旧B13仅0号步枪的三场景成长分，均不代表本轮
“移动清潮为主、Boss与条件收益单列”的质量判断。原源码、原JSON和原断言保留；
记录其针对当前代码的失配结果，不将旧JSON重新盖章成当前数据。

CI中的B12Strength/B13Strength将显式传入`--b14`：检查新的B14Evidence，默认无参数仍运行
原历史契约。新检查绑定完整源文件/数据manifest、24×2核心覆盖、全部三把参数变化的复测、
当前计算参数与记录参数一致性、真实走位/发射/命中；传说在该普通怪潮固定条件下至少
裸装75/后期85杀/15秒。该门槛是持续处理补怪压力，不是把新GPS权重调到旧排序。
史诗的单体/聚拢/地形取舍在24行报告明列，不宣称每个史诗在每个场景都压过稀有。

成长检查要求2304项无重复无遗漏、24枪射速天赋都有实际属性作用路径，辅以B14Growth、
B14Rail、B13UpgradeEffects、B13TalentEffects、M4Talents的实弹/真实事件断言。
普通追击目标通过21/25/29/35/39真实遭遇的出生比例与按存活时间积分比例均≥80%检查，
同时保留正常HP移动生存结果、原总敌数上限/安全出生/锁定/墙阻/伤害不重复契约。

固定技术性能另用Windows Forward+/Vulkan、106秒完整时间线、同一10区/2.5秒及B04终极/5秒
负载配对；统计15..105秒，原p95/p99/代理/长帧门槛不变。不得用新玩法怪死得快来解释该对照。
真实逐帧CPU/GPU仍N/A，代理按每秒发布最大值报告；一对不证明可重复性，历史Web失败仍保留。
